---
title: "Message API"
slug: /openapi/message
openapi: "./message-api.yaml"
---

# Message API 🧾

The following endpoints are managed by our service:

- `POST /messages` – save a new message
- `POST /inject` – inject a custom payload
- `GET /messages` – query messages (with optional filters)

<APIPage document="rest.yaml" />
